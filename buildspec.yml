version: 0.2

phases:
  build:
    - pwd
    - /reservation
    - mvn install
    - mvn package
artifacts:
  files:
    - target/*.jar
    - scripts/*.sh
    - appspec.yml
  discard-paths: yes
 
cache:
  paths:
    - '/root/.m2/**/*'
 